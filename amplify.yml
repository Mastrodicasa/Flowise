version: 1
frontend:
  phases:
    preBuild:
      commands:
        - 
    build:
      commands:
        - yarn; yarn install; yarn build;
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
