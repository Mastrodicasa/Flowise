version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g flowise
    build:
      commands:
        - npx flowise start
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
